<%= form_for(@making_a_difference) do |f| %>
  <% if @making_a_difference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@making_a_difference.errors.count, "error") %> prohibited this making_a_difference from being saved:</h2>

      <ul>
      <% @making_a_difference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
