<%= form_for(@website_pdf) do |f| %>
  <% if @website_pdf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@website_pdf.errors.count, "error") %> prohibited this website_pdf from being saved:</h2>

      <ul>
      <% @website_pdf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :file_name %><br />
    <%= f.select :file_name, WebsitePdf::FILENAMES, :prompt => "Select a File Name" %><br>
  </div>
  <div class="field">
    <%= f.label :file %><br />
    <%= f.file_field :file %>
  </div>

  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
